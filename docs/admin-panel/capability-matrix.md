# Capability matrix

Every operation the panel offers, its capability id, the endpoint behind it, and
the tier the **server** enforces.

Derived from `capability-map.json`, which is generated by
`backend/tools/derive-capability-map.py` from the live router. Where this
document and the map disagree, the map is right and this document is a bug.

## Reading it

- **Tier** — enforced by `RequireAccessLevel` unless marked otherwise
- **⚠ policy-only** — not enforced by route middleware; see the note
- **Subject** — a non-admin exception the policy allows

Capability ids are `domain.action`, matching the `CAPABILITY` map keys.

---

## Reads

Every read below is available to all four tiers. They carry no capability id:
reaching the screen is the capability, and route metadata governs that.

`trips` · `trips/{id}` · `trips/{id}/live` · `trips/{id}/eta` ·
`trips/{id}/corrections` · `trips/{id}/stops/{id}/manifest` · `buses` ·
`buses/{id}` · `buses/{id}/service-readiness` · `buses/{id}/inspections` ·
`buses/{id}/documents` · `fleet/documents/expiring` · `inspections/{id}` ·
`drivers` · `drivers/{id}` · `students` · `students/{id}` · `incidents` ·
`incidents/{id}` · `incidents/types` · `evidence/{id}` · `maintenance-tickets` ·
`maintenance-tickets/{id}` · `preventive-maintenance` · `replacements` ·
`replacements/{id}` · `consolidations` · `consolidations/{id}` ·
`consolidations/candidates` · `attendance-discrepancies` · `routes` ·
`routes/{id}/stops` · `schedules` · `service-calendar` · `announcements` ·
`notification-log` · `notification-log/health` · `notifications` ·
`reports/*` (six) · `users` · `users/{id}`

Two exceptions, gated above VIEWER:

| Capability | Endpoint | Tier |
|---|---|---|
| `audit.view` | `GET /audit-logs`, `/audit-logs/{id}` | SUPER_ADMIN |
| `audit.accessLog` | `GET /data-access-logs`, `/retention-runs` | SUPER_ADMIN |
| `geo.lookup` | `GET /geo/*` | OPERATIONS — route-building, unused by the panel |

---

## SUPPORT

| Capability | Endpoint | Tier | Surface |
|---|---|---|---|
| `incident.acknowledge` | `POST /incidents/{id}/acknowledge` | SUPPORT | A9 header |
| `incident.resolve` | `POST /incidents/{id}/resolve` | SUPPORT | A9 header |
| `maintenance.open` | `POST /maintenance-tickets` | SUPPORT | A10, A11 (from a failed inspection) |
| `maintenance.assign` | `POST /maintenance-tickets/{id}/assign` | SUPPORT | A10 drawer |
| `maintenance.schedule` | `POST /maintenance-tickets/{id}/schedule` | SUPPORT | A10 drawer |
| `maintenance.start` | `POST /maintenance-tickets/{id}/start` | SUPPORT | A10 drawer |
| `replacement.dispatch` | `POST /replacements/{id}/dispatch` | SUPPORT | A9 replacement panel |
| `replacement.arrived` | `POST /replacements/{id}/arrived` | SUPPORT | A9 replacement panel |
| `attendance.review` | `POST /attendance-discrepancies/{id}/review` | SUPPORT | Attendance queue |
| `notification.resend` | `POST /notification-log/{id}/resend` | SUPPORT | A13 delivery panel |

## OPERATIONS

| Capability | Endpoint | Tier | Surface |
|---|---|---|---|
| `incident.close` | `POST /incidents/{id}/close` | OPERATIONS | A9 header |
| `maintenance.complete` | `POST /maintenance-tickets/{id}/complete` | OPERATIONS | A10 drawer |
| `maintenance.cancel` | `POST /maintenance-tickets/{id}/cancel` | OPERATIONS | A10 drawer |
| `replacement.approve` | `POST /replacements/{id}/approve` | OPERATIONS | A9 |
| `replacement.reject` | `POST /replacements/{id}/reject` | OPERATIONS | A9 |
| `trip.correct` | `POST /trips/{id}/corrections` | OPERATIONS | A4 |
| `trip.cancel` | `POST /trips/{id}/cancel` | OPERATIONS | A3, A4 |
| `trip.reassign` | `POST /trips/{id}/reassign` | OPERATIONS | A4 |
| `trip.create` | `POST /trips`, `/trips/generate` | OPERATIONS | Post-MVP |
| `bus.changeStatus` | `PATCH /buses/{id}/status` | OPERATIONS | A7 — return to service |
| `bus.manage` | `POST /buses`, `PUT,DELETE /buses/{id}` | OPERATIONS | A7 |
| `bus.documents` | `POST /buses/{id}/documents`, `PUT,DELETE /buses/{busId}/documents/{documentId}` | OPERATIONS | A7 |
| `driver.manage` | `POST /drivers`, `PUT,DELETE /drivers/{id}` | OPERATIONS | A8 |
| `driver.assignBus` | `POST,DELETE /drivers/{id}/assign-bus` | OPERATIONS | A8 |
| `student.manage` | `POST /students`, `DELETE /students/{id}`, `POST,DELETE /students/{id}/assign-transport`, `PATCH /students/{id}/status` | OPERATIONS | A12 |
| `announcement.manage` | `POST /announcements`, `PUT /announcements/{id}` | OPERATIONS | A14 |
| `announcement.publish` | `POST /announcements/{id}/publish`, `/withdraw` | OPERATIONS | A14 |
| `consolidation.create` | `POST /consolidations` | OPERATIONS | Consolidations |
| `consolidation.approve` | `POST /consolidations/{id}/approve` | OPERATIONS | Consolidations |
| `consolidation.reject` | `POST /consolidations/{id}/reject` | OPERATIONS | Consolidations |
| `consolidation.notify` | `POST /consolidations/{id}/notify` | OPERATIONS | Consolidations |
| `consolidation.execute` | `POST /consolidations/{id}/execute` | OPERATIONS | Consolidations |
| `preventive.create` | `POST /preventive-maintenance` | OPERATIONS | A10 tab |
| `preventive.delete` | `DELETE /preventive-maintenance/{id}` | OPERATIONS | A10 tab |
| `network.manage` | `routes`, `schedules`, `service-calendar` writes | OPERATIONS | Post-MVP |

## SUPER_ADMIN

| Capability | Endpoint | Tier | Surface |
|---|---|---|---|
| `account.create` | `POST /users` | SUPER_ADMIN | Accounts |
| `account.setActive` | `PATCH /users/{id}/status` | SUPER_ADMIN | Accounts |
| `account.edit` | `PUT /users/{id}` | SUPER_ADMIN ⚠ subject | Accounts |
| `personalData.export` | `POST /users/{id}/subject-access-export` | SUPER_ADMIN | Audit |
| `audit.view` | `GET /audit-logs*` | SUPER_ADMIN | Audit |
| `audit.accessLog` | `GET /data-access-logs`, `/retention-runs` | SUPER_ADMIN | Audit |

## Subject-scoped — policy, not middleware

Enforced in the policy because route middleware cannot express "tier X **or**
the subject". The route table understates these; the server is stricter.

| Capability | Endpoint | Subject | Tier |
|---|---|---|---|
| `account.edit` | `PUT /users/{id}` | the user themselves | SUPER_ADMIN |
| `student.edit` | `PUT /students/{id}` | the student themselves | OPERATIONS |
| `driver.setStatus` | `PATCH /drivers/{id}/status` | the driver themselves | OPERATIONS |

---

## Driver-shared endpoints — policy-enforced (G3-3, fixed)

These carry no role gate, because the driver app must reach them from a bus.
Their policies now ask for the driver doing their own job **or** an
administrator at the tier the operation costs. The route table understates
them; the server is stricter.

| Endpoint | Subject exception | Tier |
|---|---|---|
| `POST /trips/{id}/start` | the assigned driver | OPERATIONS |
| `POST /trips/{id}/complete` | the assigned driver | OPERATIONS |
| `POST /trips/{id}/positions` | the assigned driver | OPERATIONS |
| `POST /trips/{id}/board`, `/alight` | the assigned driver | OPERATIONS |
| `POST /trips/{id}/stops/{id}/arrive`, `/skip` | the assigned driver | OPERATIONS |
| `POST /trips/{id}/left-behind` | the assigned driver | OPERATIONS |
| `POST /buses/{id}/inspections` | any driver | OPERATIONS |
| `POST /incidents` | any driver | SUPPORT |
| `POST /incidents/{id}/notes` | the reporter | SUPPORT |
| `POST /incidents/{id}/cancel` | the reporter | SUPPORT |
| `POST /evidence` | any driver | SUPPORT |

**No panel screen offers the trip-operation endpoints.** Boarding students and
marking arrivals from an office laptop would destroy the evidence value of the
attendance record, whoever is permitted to do it.

**No panel screen offers the driver-operational endpoints.** Boarding students
and marking arrivals from an office laptop would destroy the evidence value of
the attendance record, whoever is permitted to do it.
