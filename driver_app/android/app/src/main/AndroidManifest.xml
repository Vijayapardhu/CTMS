<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Evidencing a failed safety-critical inspection item. Declared because
         the app gates on it explicitly: without the declaration the runtime
         check can never be granted, and a driver would be told the camera is
         switched off on a handset where it is not. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Not required: a handset with no camera can still sign in, read a trip
         and pass an inspection. Only a failing critical item needs one. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- M3. Coarse is declared alongside fine because a driver may grant only
         the approximate one: the office still learns roughly where the bus is,
         which is worth more than refusing to track at all. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- A trip runs for ninety minutes with the phone in a cradle and the
         screen off. Without a foreground service the OS stops delivering
         fixes, and the gap would look to the office like a bus that vanished.
         Deliberately NOT ACCESS_BACKGROUND_LOCATION: the service runs only
         while a trip is running and the driver can see it in the shade. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="ctms_driver"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Substituted from android/local.properties by app/build.gradle.kts.
             The literal value is never committed. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
